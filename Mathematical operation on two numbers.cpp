#include<stdio.h>
int main()
{
	int a,b;
	scanf("%d%d",&a,&b);
	printf("the sum of two numbers is%d\n",a+b);
	printf("the difference of two numbers is%d\n",a-b);
	printf("the product of two numbers is%d\n",a*b);
	printf("the ratio of two numbers is%d\n",a/b);
	return 0;
}
